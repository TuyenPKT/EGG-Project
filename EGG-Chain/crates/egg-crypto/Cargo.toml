[package]
name = "egg-crypto"
version = "0.0.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
blake3 = "1.5"
egg-types = { path = "../egg-types" }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
rand = "0.8"
