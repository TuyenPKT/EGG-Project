[package]
name = "egg-net"
version = "0.0.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
egg-types = { path = "../egg-types" }
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"

[dev-dependencies]
rand = "0.8"
